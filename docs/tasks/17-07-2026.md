## Bussiness logic 

[] Organisation - When we remove user, we should remove user from org member as well as from database and should delete all his sessions.
[] Codebase - Remove my default mail which sends no reply mail. From now own use organisation specific mail to send mail. if inviting from emailder.dev then mail from no-reply@emailer.dev or doota@emailer.dev. e.g. domain: domain.tld -> mailId: doota@domain.tld

## UI

[] Organisation - We should add sub domain provisioning so user can add a sub domain mail system.
[] Codebase - Prefer to use Datatables when possible, they provide better functionalities and in action section use button group.
[] Codebase - Use full width on whole and and still make the app responsive according screen size and some places you can use container sizes too.
[] Add basic email templates - Don't use `better-svelte-mail`, it have some problems. Use different provider or something we can diccuss that. This task can be added to different sections. I would prefer html templates or something we can comeup with and use `@ethercorps/un-jinja` for templating


# Rule

- You should break them in subtasks and discuss with me before implementing. There might be scenarios where one of my suggestion can also relates to other things and we can improve them too.
- Suggest Improvement for each task before implementation.
- We need to come up with a task system for you, where i can define task and you can pull the task from it and follow rule 1 and 2.
