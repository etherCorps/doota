{
  "name": "@doota/mail-in",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "gen": "wrangler types",
    "check": "wrangler deploy --dry-run --outdir .wrangler/dry"
  },
  "dependencies": {
    "@doota/mail-core": "workspace:*"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
